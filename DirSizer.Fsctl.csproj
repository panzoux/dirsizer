<Project Sdk="Microsoft.NET.Sdk">
  <!-- dirsizer-fsctl.exe: folder-size scan through FSCTL_GET_NTFS_FILE_RECORD. The reference implementation. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <AssemblyName>dirsizer-fsctl</AssemblyName>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AllowUnsafeBlocks>false</AllowUnsafeBlocks>
    <PublishAot>true</PublishAot>
    <InvariantGlobalization>true</InvariantGlobalization>
    <DebugType>none</DebugType>
    <StripSymbols>true</StripSymbols>
    <OptimizationPreference>Speed</OptimizationPreference>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
  </PropertyGroup>
  <ItemGroup>
    <Compile Include="FsctlProgram.cs" />
    <Compile Include="FsctlScanner.cs" />
    <Compile Include="Cli.cs" />
    <Compile Include="DriveRoot.cs" />
    <Compile Include="SelfTests.cs" />
    <ProjectReference Include="DirSizer.Core.csproj" />
  </ItemGroup>
</Project>